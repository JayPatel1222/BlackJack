{
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "nanoid": "^5.1.6",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  },
  "type": "module",
  "scripts": {
    "dev": "nodemon server.js",
    "postinstall": "npm run build-client",
    "build-client": "cd ../client && npm install && ng build --configuration production && rm -rf ../server/public/blackjack/browser && mkdir -p ../server/public/blackjack/browser && cp -r dist/blackjack/browser/* ../server/public/blackjack/browser/",
    "start": "node server.js"
  }
}